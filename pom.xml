<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.example</groupId>
  <artifactId>demo</artifactId>
  <version>1.0-SNAPSHOT</version>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.release>21</maven.compiler.release>

    <javafx.version>21.0.5</javafx.version>
    <sqlite.jdbc.version>3.46.1.0</sqlite.jdbc.version>
    <slf4j.version>2.0.13</slf4j.version>

    <!-- App-Metadaten -->
    <app.name>TodoListeZbW</app.name>
    <app.version>1.0.0</app.version>

    <!-- optional: Icon (muss existieren), sonst Zeile entfernen -->
    <!-- <app.icon>${project.basedir}/src/main/resources/icon.ico</app.icon> -->
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.slf4j</groupId>
        <artifactId>slf4j-bom</artifactId>
        <version>${slf4j.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <!-- JavaFX -->
    <dependency>
      <groupId>org.openjfx</groupId>
      <artifactId>javafx-controls</artifactId>
      <version>${javafx.version}</version>
    </dependency>
    <dependency>
      <groupId>org.openjfx</groupId>
      <artifactId>javafx-fxml</artifactId>
      <version>${javafx.version}</version>
    </dependency>

    <!-- SQLite -->
    <dependency>
      <groupId>org.xerial</groupId>
      <artifactId>sqlite-jdbc</artifactId>
      <version>${sqlite.jdbc.version}</version>
    </dependency>

    <!-- Logging -->
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-simple</artifactId>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!-- Compiler -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
        <configuration>
          <release>${maven.compiler.release}</release>
        </configuration>
      </plugin>

      <!-- javafx:run (nur Dev) -->
      <plugin>
        <groupId>org.openjfx</groupId>
        <artifactId>javafx-maven-plugin</artifactId>
        <version>0.0.8</version>
        <configuration>
          <mainClass>com.example/com.example.App</mainClass>
        </configuration>
      </plugin>

      <!-- Dependencies nach target/app/lib -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <version>3.7.1</version>
        <executions>
          <execution>
            <id>copy-deps</id>
            <phase>package</phase>
            <goals>
              <goal>copy-dependencies</goal>
            </goals>
            <configuration>
              <includeScope>runtime</includeScope>
              <outputDirectory>${project.build.directory}/app/lib</outputDirectory>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- Haupt-JAR nach target/app -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-resources-plugin</artifactId>
        <version>3.3.1</version>
        <executions>
          <execution>
            <id>copy-jar</id>
            <phase>package</phase>
            <goals>
              <goal>copy-resources</goal>
            </goals>
            <configuration>
              <outputDirectory>${project.build.directory}/app</outputDirectory>
              <resources>
                <resource>
                  <directory>${project.build.directory}</directory>
                  <includes>
                    <include>${project.build.finalName}.jar</include>
                  </includes>
                </resource>
              </resources>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- jpackage EXE Installer -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>3.3.0</version>
        <executions>
          <execution>
            <id>jpackage-exe</id>
            <phase>package</phase>
            <goals>
              <goal>exec</goal>
            </goals>
            <configuration>
              <executable>jpackage</executable>
              <environmentVariables>
                <PATH>${env.PATH};C:\Program Files (x86)\WiX Toolset v3.14\bin</PATH>
              </environmentVariables>

              <arguments>
                <!-- Installer -->
                <argument>--type</argument><argument>msi</argument>
                <argument>--dest</argument><argument>${project.build.directory}/installer</argument>
                <argument>--name</argument><argument>${app.name}</argument>
                <argument>--app-version</argument><argument>${app.version}</argument>
                <argument>--icon</argument>
                <argument>${project.basedir}/src/main/resources/HakenIcon.ico</argument>

                <!-- Eingabe: unser app Ordner -->
                <argument>--input</argument><argument>${project.build.directory}/app</argument>

                <!-- Startpunkt: klassisch (nicht -jar), weil JavaFX Module -->
                <argument>--main-jar</argument><argument>${project.build.finalName}.jar</argument>
                <argument>--main-class</argument><argument>com.example.App</argument>

                <!-- JavaFX zur Laufzeit -->
                <argument>--java-options</argument><argument>--module-path</argument>
                <argument>--java-options</argument><argument>app/lib</argument>
                <argument>--java-options</argument><argument>--add-modules</argument>
                <argument>--java-options</argument><argument>javafx.controls,javafx.fxml</argument>

                <!-- Encoding -->
                <argument>--java-options</argument><argument>-Dfile.encoding=UTF-8</argument>

                <!-- Windows UX -->
                <argument>--win-shortcut</argument>
                <argument>--win-menu</argument>

                <!-- optional: per-user install ohne Admin -->
                <argument>--win-per-user-install</argument>
              </arguments>
            </configuration>
          </execution>
        </executions>
      </plugin>

    </plugins>
  </build>
</project>
